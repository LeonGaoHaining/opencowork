# Changelog Mirror

The canonical changelog lives in the repository root: `CHANGELOG.md`.

This file exists only as a lightweight pointer for readers browsing inside `docs/`.

Current release line: `v0.14.9`.

For release notes and verification details, use the root `CHANGELOG.md` and GitHub Releases.
